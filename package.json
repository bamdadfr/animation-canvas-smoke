{
  "name": "animation-canvas-smoke",
  "version": "2.0.0",
  "private": false,
  "description": "Smoke Animation with Canvas",
  "repository": {
    "url": "https://github.com/bamdadsabbagh/animation-canvas-smoke"
  },
  "license": "MIT",
  "author": {
    "name": "Bamdad Sabbagh",
    "email": "hello@bamdadsabbagh.com",
    "url": "https://bamdadsabbagh.com"
  },
  "scripts": {
    "build": "yarn build:clean && yarn build:run",
    "build:clean": "rimraf dist/",
    "build:run": "yarn vite build",
    "build:serve": "serve -s dist/",
    "dev": "vite",
    "lint": "eslint src --ext .js,.jsx,.ts,.tsx --fix",
    "package:clean": "rimraf node_modules",
    "package:reinstall": "npm run package:clean && yarn",
    "package:sort": "npx sort-package-json"
  },
  "dependencies": {},
  "devDependencies": {
    "@bamdadsabbagh/eslint-config": "^1.17.2",
    "@parcel/transformer-sass": "^2.0.1",
    "rimraf": "^3.0.2",
    "sass": "^1.43.4",
    "sass-reset": "^1.0.4",
    "vite": "^2.6.13"
  }
}
